{
  "type": "mongodb",
  "host": "localhost",
  "port": 27017,
  "database": "RemoteCalculator",
  "synchronize": true,
  "logging": false,
  "entities": ["src/entities/**/*.entity.ts"],
  "migrations": ["src/migration/**/*.ts"],
  "subscribers": ["src/subscriber/**/*.ts"],
  "cli": {
    "entitiesDir": "src/entities",
    "migrationsDir": "src/migration",
    "subscribersDir": "src/subscriber"
  }
}
